package com.windlogs.tickets.enums;

/**
 * Enum representing the possible complexity levels of a solution
 */
public enum ComplexityLevel {
    LOW,        // Simple solution, easy to implement
    MEDIUM,     // Moderate complexity, requires some effort
    HIGH,       // Complex solution, requires significant effort
    VERY_HIGH   // Extremely complex solution, requires extensive effort and planning
}
